name: prospector-zmk-module
build:
  cmake: .
  kconfig: Kconfig
  settings:
    board_root: .
    dts_root: .
  depends:
    - lvgl
  # drivers/ and include/ are picked up automatically via cmake: .
  # dts/bindings/ is found because dts_root: . is set above.